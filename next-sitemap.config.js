const config = {
  siteUrl: "https://hollislandscaping.vercel.app",
  generateRobotsTxt: true, // (Optional parameter for creating robots.txt file)
  // Other available options..
}

module.exports = config
